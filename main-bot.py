- Load stream 
- setup analysis loop (compare each chunk with the last to detect if song has changed, if it has, new song record created.)
    > NetilBot is ignorant of tracklists, uses timestamps, processor of track id's will group into schedueele (human or robot) 
    > Chunks need to be large enough to handle DJ mixes / blends. Or small enough? - Needs thought
    > Write new track to external store. Format? Connectivity needs to be easy for MainWindowWrapper
